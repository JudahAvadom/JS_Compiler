#ifndef IO_H
#define IO_H 

/*
 * This function read the file input and return the content of the file
 * @param filename The filename for be compiled
 * @return buffer The content of the file 
*/
char* read_file(const char* filename);

#endif